import React from 'react';
import useTitle from '../../hook/useTitle';

const Blogs = () => {
    useTitle("Blogs")
    return (
        <div>
            this is blogs page
        </div>
    );
};

export default Blogs;